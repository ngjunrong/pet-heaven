import React from "react";

// import components
import HeroSlider from "../components/HeroSlider";

const Hero = () => {
  return (
    <section>
      <HeroSlider />
    </section>
  );
};

export default Hero;
